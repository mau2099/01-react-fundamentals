name: continuos integration
on:
  pull_request:
    branches:
      - 'main'
jobs:
  validate-unit-test:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        node: [14]
    runs-on: ${{ matrix.os }}
    steps:
      - name: ▶️ Run unit tests
        run: |
          npm ci
          npm run test
